spring:
  profiles:
    default: local-tunnel
---
spring:
  config:
    activate:
      on-profile: local
---
spring:
  config:
    activate:
      on-profile: local-tunnel
---
spring:
  config:
    activate:
      on-profile: dev